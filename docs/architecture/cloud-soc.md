# Cloud SOC Architecture

The cloud SOC receives probe telemetry, schedules approved workflows, stores normalized findings, and exposes customer and technician interfaces. Production topology, data retention, and tenant isolation controls must be documented before go-live.
